# OS/editor noise
.DS_Store
.idea/

# Local Solana / Anchor validator state
test-ledger/

# Anchor local cache (when present)
.anchor/

# Secrets at repo root (do not use `.env*` — would ignore `.env.example`)
.env
.env.local
.env.*.local

# Circom Powers of Tau at workspace root (large; fetch via script or docs)
*.ptau

# Logs
*.log

# Rust/Cargo build outputs
target/**

# Keep Anchor deploy keypairs versioned to preserve program IDs across machines
!target/deploy/
!target/deploy/*-keypair.json

v2.md
guide.md
I.md
n.md