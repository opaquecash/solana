# Copy this file to .env and fill in values. Do not commit .env.


# Cluster: devnet | testnet | mainnet-beta | localnet
# localnet = http://127.0.0.1:8899 (run: solana-test-validator)
# VITE_SOLANA_CLUSTER=devnet
# Optional override for any cluster (Solana JSON-RPC):
# VITE_SOLANA_RPC_URL=https://api.devnet.solana.com
# Legacy name; must be a Solana RPC.
# VITE_RPC_URL=

# --- Multichain (Ethereum / Sepolia) ---
# Sepolia JSON-RPC for EVM send, UAB relay, EVM PSR, and cross-chain scan.
# VITE_SEPOLIA_RPC_URL=https://ethereum-sepolia.publicnode.com
# Override the stealth cryptography WASM module URL (default: hosted on opaque.cash).
# VITE_WASM_URL=https://www.opaque.cash/pkg/cryptography.js

